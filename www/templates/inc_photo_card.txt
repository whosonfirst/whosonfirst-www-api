<div class="card sfomuseum-photo-card" data-photo-id="{$photo.id|@escape}" id="photo-card-{$photo.id|@escape}">

     <a href="{$cfg.abs_root_url}photos/{$photo.id|@escape}/"><img id="photo-img-{$photo.id|@escape}" class="card-img-top photo-{$photo.status_id|@whosonfirst_photos_status_id_to_label}" src="{$cfg.abs_root_url}static/{$photo|@whosonfirst_photos_photo_to_relpath}" alt="" /></a>
     <div class="card-body">

     	{if 0}
     	<form>
	  <div class="form-group">
	      <label for="title">Title</label>
	      <input type="text" class="form-control" id="title" placeholder="">
  	      </div>
	 <div class="form-group">
    	      <label for="caption">Caption</label>
    	      <input type="text" class="form-control" id="caption" placeholder="">
  	 </div>
	 </form>
	 {/if}

     </div>
     <div class="card-footer">

          <div class="card-text">This photo (<code>{$photo.id|@escape}</code>) was uploaded on {$photo.created|@date_format|@escape} by <a href="#">[USER ID {$photo.user_id|@escape}]</a>.</div>

	  {if 0}
	  <div class="card-text">
	  {if $photo.status_id==0}
	  This photo is private. 
 	  {elseif $photo.status_id==1}
	  This photo is public.
	  {else}{/if}
	  </div>
	  {/if}
		
	  {if $cfg.user|@users_acl_has_capability:"can_edit_photos" || $cfg.user|@users_acl_has_capability:"can_delete_photos"}
	  <div>

	  {if $cfg.user|@users_acl_has_capability:"can_edit_photos"}
	  
	  <button class="btn btn-sm btn-info photo-mk-public" id="photo-mk-public-{$photo.id|@escape}" data-photo-id="{$photo.id|@escape}" {if $photo.status_id==1}style="display:none;"{/if}>Make this photo public</button>

	  <button class="btn btn-sm btn-warning photo-mk-private" id="photo-mk-private-{$photo.id|@escape}" data-photo-id="{$photo.id|@escape}" {if $photo.status_id==0}style="display:none;"{/if}>Make this photo private</button>

	  {/if}

	  {if $cfg.user|@users_acl_has_capability:"can_delete_photos"}
	  <button class="btn btn-sm btn-danger photo-delete" data-photo-id="{$photo.id|@escape}">Delete this photo</button>
	  {/if}

	  </div>
	  {/if}

     </div>

</div>
