<form action="{$cfg.abs_root_url}search">
  <div class="form-row align-items-center">
  
	<div class="col-auto">
	     	<label class="sr-only" for="q">Query</label>
		<input type="search" name="q" value="{$query|escape}" placeholder="Search {$cfg.site_name|@escape}" class="form-control">
	</div>
	
	<div class="col-auto">
	     	<label class="sr-only" for="scope">Scope</label>		
		<select name="scope" class="form-control">
			<option value="names"{if $scope == 'names'} selected="selected"{/if}>All names</option>
			<option value="default"{if $scope == 'default'} selected="selected"{/if}>Default names</option>
			<option value="preferred"{if $scope == 'preferred'} selected="selected"{/if}>Preferred names</option>
			<option value="alt"{if $scope == 'alt'} selected="selected"{/if}>Variant names</option>
		</select>
	</div>

	<div class="col-auto">
	     	<label class="sr-only" for="search">Search</label>	
		<input type="submit" value="search" class="btn btn-light form-control">
	</div>

 </div>
 
</form>
